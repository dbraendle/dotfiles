# Mount Configuration for macOS autofs
# Managed by dotfiles mount-setup.sh
#
# Format: INI-style configuration
# Each section defines a mount point
#
# Available options:
#   type     = nfs, smb (future: afp, etc.)
#   server   = hostname or IP address
#   path     = remote path on server
#   options  = mount options (comma-separated)
#   local    = local mount point (will be created)
#   enabled  = true/false (allows disabling without deletion)
#
# Example NFS mount:
# [Medien]
# type=nfs
# server=192.168.1.100
# path=/mnt/storage/media
# options=resvport,hard,intr,rsize=32768,wsize=32768,proto=tcp,timeo=900
# local=/Volumes/Media
# enabled=true

# Example: Additional NFS mount (disabled)
# [Backup]
# type=nfs
# server=192.168.178.10
# path=/backup
# options=soft,intr,rsize=32768,wsize=32768
# local=/Volumes/Backup
# enabled=false

# Example: SMB mount (future implementation)
# [FileServer]
# type=smb
# server=fileserver.local
# path=/shared
# options=vers=3.0,sec=ntlmssp
# local=/Volumes/FileServer
# enabled=false

# Notes:
# - Mount points will be created automatically if they don't exist
# - Use 'enabled=false' to temporarily disable a mount
# - Changes take effect after running ./mount-setup.sh
# - Config is portable across different Macs via dotfiles